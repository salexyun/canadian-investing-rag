# Pin exact versions once finalized (reproducibility criterion).

# ingestion — fetch step (ingestion/fetch.py)
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
playwright==1.62.0
# after `pip install -r requirements.txt`, also run: playwright install chromium

# ingestion — chunk + load step (ingestion/pipeline.py, not yet implemented)
# dlt

# retrieval / knowledge base
# qdrant-client
# rank-bm25          # or reuse course's minsearch

# LLM
# openai

# eval
# pandas
# scikit-learn

# interface
streamlit

# monitoring
# psycopg2-binary
