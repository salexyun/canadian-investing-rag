# Pin exact versions once finalized (reproducibility criterion).

# ingestion — fetch step (ingestion/fetch.py)
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
playwright==1.62.0
# after `pip install -r requirements.txt`, also run: playwright install chromium

# ingestion — chunk + load step (ingestion/pipeline.py)
# dlt              # wrapping fetch+chunk as dlt resources, for ingestion-pipeline rubric credit

# retrieval / knowledge base
# qdrant-client
# rank-bm25          # BM25, kept as a distinct comparable method alongside vector + hybrid

# LLM
# openai

# eval
# pandas

# interface
streamlit

# monitoring / observability / eval — Langfuse Cloud (free tier), not self-hosted
# langfuse
